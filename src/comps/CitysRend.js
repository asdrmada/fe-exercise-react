import React, { Component } from 'react'

export class CitysRend extends Component {
    render() {
        return (
            <div>
                {/* Layout used with the component for rendering cities would be same as that used within
                the Swansea component. Aligned vertically and predominantly centered.  */}
            </div>
        )
    }
}

export default CitysRend
